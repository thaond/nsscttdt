<?xml version="1.0"?>

<custom-sql>
	<sql id="com.sgs.portlet.nation.service.persistence.NationFinder.findBy_TEN_MA_MOTA_QUOCGIA">
		<![CDATA[
			SELECT
				pml_nation.*
			FROM
				pml_nation
			WHERE
				1 = 1
				AND (
					(lower(pml_nation.nationcode) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(pml_nation.nationname) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
				 	(lower(pml_nation.description) LIKE ? [$AND_OR_NULL_CHECK$])				 	
				) 
		]]>
	</sql>
	<sql id="com.sgs.portlet.nation.service.persistence.NationFinder.countBy_TEN_MA_MOTA_QUOCGIA">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				pml_nation
			WHERE
				1 = 1
				AND (
					(lower(pml_nation.nationcode) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(pml_nation.nationname) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
				 	(lower(pml_nation.description) LIKE ? [$AND_OR_NULL_CHECK$])				 	
				) 
		]]>
	</sql>
</custom-sql>
<?xml version="1.0"?>

<custom-sql>
	<sql id="com.sgs.portlet.document.manageworkflow.service.persistence.PmlManageWorkDocumentFlowFinder.countByDocumentWorkflow">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				pml_manage_documentworkflow 
			WHERE
				(lower(workflowname) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
				(lower(description) LIKE ? [$AND_OR_NULL_CHECK$])
		]]>
	</sql>
	<sql id="com.sgs.portlet.document.manageworkflow.service.persistence.PmlManageWorkDocumentFlowFinder.findByDocumentWorkflow">
		<![CDATA[
			SELECT
				pml_manage_documentworkflow.*
			FROM
				pml_manage_documentworkflow 
			WHERE
				(lower(workflowname) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
				(lower(description) LIKE ? [$AND_OR_NULL_CHECK$])
		]]>
	</sql>
</custom-sql>